<div class='effect-tray-container'>
  <div class='effect-tray'>

    {{#each effectsTray.effects as |effect|}}

      <img class='effect-tray-icon' src='{{effect.img}}' title='{{effect.name}}' />
    {{/each}}
  </div>

</div>