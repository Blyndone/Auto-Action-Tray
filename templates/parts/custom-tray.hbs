  {{log "VVV"}}
{{log tray.id}}
{{log @root.currentTray.id}}
{{log "^^^"}}  

{{#each customTrays as |tray|}}
<div
  class=
  {{#if (eq @root.currentTray.id tray.id)}} 'custom-tray-active' 
    {{else}}
  'custom-tray-button'
  {{/if}}
  data-id={{tray.id}}
  data-action='setTray'
  data-type='custom'
>{{capitalize tray.id}}</div>
{{/each}}